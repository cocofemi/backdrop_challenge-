import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
    headerText: {
        fontSize: 16,
        fontWeight: '600',
        marginTop: 80,
        marginLeft: 25
    },
})